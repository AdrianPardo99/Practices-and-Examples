#include <stdio.h>
#include <stdlib.h>
#include "Lista.h"
int main(){
	
	/*Lista l=cons(1,cons(2,cons(3,cons(4,cons(5,vacia()))))),
	l1=cons(6,(cons(7,vacia()))),
	l2=cons(5,cons(-1,cons(0,vacia())));
	impElem(cabeza(l));
	impElem(cabeza(resto(l)));
	impLista(l);
	impNum(numElem(l));
	l=(concat(l,l1));
	impNum(numElem(l));
	impLista((concat(l,l1)));
	impLista(invierte(concat(l,l1)));
	impLista(InsOrd(20,l));
	impLista(InsOrd(20,InsOrd(10,InsOrd(45,InsOrd(5,vacia())))));
	impListaR(l);
	impNum(numElem(l));
	impLista(OrdList(concat(l2,concat(l1,l))));*/
	return 0;
}
